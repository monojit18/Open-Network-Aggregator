{
    "name": "bigquery_app",
    "description": "Wrapper around GCP Bigquery API",
    "version": "1.0.0",
    "private": true,
    "license": "Apache",
    "author": "Monojit Datta",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "preinstall": "mkdir -p data",
        "install": "npm install --save-dev express jshint dotenv cors @google-cloud/bigquery",
        "start": "node bigquery.js"
    },
    "engines": {
        "node": ">=16.0.0"
    },
    "devDependencies": {
        "@google-cloud/bigquery": "^8.1.1",
        "cors": "^2.8.5",
        "dotenv": "^17.2.1",
        "express": "^5.1.0",
        "jshint": "^2.13.6"
    }
}
